---
version: 3
workflows: 
  testing: 
    plan: 
      steps: 
        - init
        - run: "terraform plan -input=false -refresh -out $PLANFILE"
        - run: "checkov -f $PLANFILE"
        - run: "printf 'print me'"
